#include "main.h"
/**
*_abs - absolute value
*@p: integer input
*Description: returns absolute value
*Return: p if p was positive, -p if it was negative
*/
int _abs(int p)
{
if (p >= 0)
{
return (p);
}
else
{
return (-p);
}
}
